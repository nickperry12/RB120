# Create the Class
# Create an empty class named Cat.

=begin

Fairly simple, we only need to define the class. We can do so using the keyword
`class` to begin the class definition, and `end` when the definition is done.

=end

class Cat
end